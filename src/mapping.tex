\begin{frame}{Mapping Greedy to Optimal}

{Consider the mapping
$\{(p, s) : p \in I, s \in I^*\}$,
where $p$ is the shortest path in $I$ that is in conflict with $s$}

\input{mapping-fig}

\end{frame}